/**
 * Empty module placeholder for renderer-only libraries
 * This prevents errors when renderer-only libraries are accidentally
 * imported in the main process during webpack bundling.
 */

module.exports = {};
